[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install"]
# postinstall script will automatically run prisma generate

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

